version: '3.4'

services:
  game:
    build: .
    ports:
      - '1234:1234'
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - db
  db:
    image: 'postgres:14'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - TZ=America/Sao_Paulo
    volumes:
      - .:/app
      - data:/var/lib/postgresql/data

volumes:
    data: